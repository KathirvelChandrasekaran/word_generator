name: word_generator
description: A dart based util package for generating random nouns, verbs and names. This package helps you to generate random words for your project without any hassle.
version: 0.3.2
homepage: https://github.com/KathirvelChandrasekaran/word_generator

environment:
  sdk: ">=2.18.6 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
